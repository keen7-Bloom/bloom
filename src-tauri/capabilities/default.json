{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for Bloom windows",
  "windows": ["main", "wallpaper"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
